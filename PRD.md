# 「解题配方库」产品需求文档（PRD）

> 版本：v1.1 ｜ 日期：2026-09-23 ｜ 负责人：就业办（暨南大学「卓越杯」参赛团队）
> 所属赛事：暨南大学管理学院企业管理系「卓越杯」人工智能创新实践大赛 · **B 应用实践赛道**
> 文档状态：修订稿（采纳"Agent 写入 / 人只读 / WorkBuddy 搭建 / 两段式脱敏"等意见）
> **2026-09-25 回写记录**（落实 v1.2 之 Q5）：① §6 **F-10 只读 MCP Server 由 P2 提升为 P0** 并标已实现；② **§8「机器可读架构」整节重写**（删去"不用自研 MCP、留 P2"的失效结论，改为当前四层静态架构 + MCP Server + 检索内核 + 防编造护栏）；③ §14 风险表同步反转"MCP/CLI 工期吞噬"一条；④ 顺带标注 F-01 写入 API 与 F-06 表彰为本期非目标，消掉与 v1.2 §3 的自相矛盾。

> 📌 **本文件是基础全量 PRD（v1.1）**。当前增量工作（F-05 经验向导 Agent + F-10 最小 MCP Server 细化、F-10 提 P0）见同目录外层的 **`解题配方库_PRD_经验向导与MCP_v1.2.md`**。两份冲突时以 v1.2 增量文件为准。

---

## 0. 一句话定位

**「解题配方库」是一个开源、机器可读的人类解题经验共享平台**：让任何用 AI 解决过问题的人，把"兜兜绕绕才解决一个问题"的**完整配方**（用了什么模型、什么 skill、什么 harness、什么 soul/Agent.md、什么电脑配置、踩过哪些死胡同、最终怎么破局）脱敏后贡献出来，**全站可供任意 Agent 直接浏览调用**，让人和 Agent 都不再重复踩已知的坑。

---

## 1. 产品背景与问题

### 1.1 痛点
- **AI 解题不稳定**：同一个问题，当前模型经常要兜兜绕绕、试错多次才能解决；这些"绕路经验"散落在个人记忆里，随问题结束而丢失。
- **经验不可复用**：别人踩过的坑，你还得再踩一遍；没有"解题过程"的结构化沉淀，只有最终答案（且答案往往不可复现）。
- **Agent 没有"集体记忆"**：现有校园 AI（清小搭/山小芽/小叶）都是问答机器人，各自孤立；没有共享的经验层让 Agent 互相学习"怎么破局"。
- **配置不可复现**：同样的 prompt，换模型/换硬件/换 skill 组合结果天差地别，但"什么组合 work"从不被记录。

### 1.2 机会（来自前序研究）
- 2026 年 AI 工程界最热方向之一是 **Agent 共享经验记忆 / 群体经验学习**（学术：Spark/Memco 论文 arXiv 2511.08301；产品：Caura、AWS AgentCore Memory、Armalo Memory Mesh）。
- 现有玩家**只给代码智能体写 runbook**，**没有面向普通用户/校园、专门捕获"含死胡同的人类解题过程"、人机共用的版本** —— 这是空白。
- `llms.txt` + Agent Skills（Anthropic 2025-12 开放标准，已被 30+ 产品采用）让"网站让 Agent 读"从概念变成可落地的真实标准。

---

## 2. 目标与成功指标

| 类型 | 目标 | 衡量 |
|---|---|---|
| 产品目标 | 建成可运行、Agent 可读取的解题经验库 MVP | 网站上线 + 演示 Agent 能查库返回经验 |
| 参赛目标 | B 赛道拿奖（场景落地价值 40% 为最高权重项） | 评委评分 / 进入决赛 |
| 内容目标 | 初赛前积累 ≥ 30 条真实配方 | 存储层计数的配方条数 |
| 机器可读目标 | 全站支持 `llms.txt` + 公开 JSON，外部 Agent 一次抓取即得全库 | `curl /api/experiences.json` 验证 |
| 验证目标 | ≥ 10 名真实学生试用并反馈"确实少走弯路" | 试用反馈表 |

---

## 3. 用户与场景

> **核心访问原则**：网站对**人是只读的**（可浏览、搜索、看详情），**写入仅限 Agent 通过写入 API 完成**，不提供人类手填表单。人贡献经验 = 让自己的 Agent 在对话中提炼并写入。

### 3.1 贡献者（人 → 经由其 Agent 写入）
- **使用 AI 解决问题的人**：任何用 AI（Hy3 / Deepseek-V4.1-flash 等）解决过真实问题的人，都是潜在贡献者。
- **写入方式**：人**不直接在网站手动投稿**；由其本人 Agent 在对话中提炼配方、脱敏后，调用本库**写入 API** 提交。人只能浏览，不能手填表单。
- **策展/审核**：就业办/技术社团以"审计 Agent + 人工抽检"做质量与合规把关，而非前置人工录入。

### 3.2 消费者（人，只读）
- 遇到同类问题的同学：来搜"别人怎么破局"。

### 3.3 消费者（Agent）← 核心差异化
- 任意接入 `llms.txt`/JSON 的 AI Agent：在解题前先查本库，避免已知死胡同。
- 本产品的演示 Agent「经验向导」：学生口述卡点 → 检索库 → 返回"破局法 + 别踩的坑"。

### 3.4 典型场景
1. 学生用 Hy3 调爬虫总被反爬，兜了 3 小时 → 其 Agent 把配方写入库：模型 Hy3 + skill(anti-detect) + 死胡同(硬刚 UA) + 解法(降频+随机头) + 硬件(8G 内存笔记本)。
2. 另一 Agent 接到"反爬"任务 → 先抓本库 → 直接避开硬刚 UA 的死胡同。

---

## 4. 核心概念定义

| 概念 | 定义 |
|---|---|
| **解题配方 (Recipe)** | 一条结构化经验记录，含：问题背景、环境(模型/skill/harness/soul/硬件)、多条**结构化死胡同**(每条含尝试/失败现象/卡了多久/本可提前避开的信号)、最终解法、复盘。死胡同为灵魂字段，详见 §7。 |
| **死胡同 (Dead-end)** | 明确标注"试过但无效"的路径，**结构化记录**（非一句话带过）；让他人省去探索成本 |
| **机器可读 (Agent-readable)** | 网站提供 `llms.txt` + 结构化 JSON，Agent 可零解析成本获取全库 |
| **脱敏 (Scrub)** | 贡献者 Agent 自检 + 本站复检，去除 PII/密钥/内网路径后公开 |
| **soul / Agent.md** | 贡献者的 Agent 人格配置（洗去个人标识后公开，作为可复现配方的一部分） |

---

## 5. 差异化与竞品分析

| 对比对象 | 它做什么 | 我们的差异 |
|---|---|---|
| 清小搭/山小芽/小叶 | 校园问答机器人 | 我们是"经验资产平台"，非问答；结构层不同 |
| Spark/Caura/AWS Memory | Agent 共享记忆（代码向） | 我们面向**人类贡献 + 校园场景 + 过程/死胡同捕获** |
| 普通踩坑帖博客 | 给人看的文章 | 我们**机器可读 + SKILL.md 兼容 + 可接入 Agent 生态** |

**诚实边界（答辩必须说清）**：不称"全球首创共享记忆"（Spark/Caura 已存在）；精准表述为"**首个校园/人类贡献/专门捕获解题过程与死胡同/机器可读的经验库**"。

---

## 6. 功能需求

### P0（MVP 必须，初赛前交付）
1. **F-01 配方写入（Agent-only）**：通过**写入 API** 由 Agent 提交一条配方（字段见 §7），不支持人类网页手填。支持匿名贡献者。<br>⏸ **本期暂缓**：v1.2 §3 已将其列为本期**非目标**（Agent 只读；写入口子一开，脱敏 / 审计 / 幂等要一起做，工期翻倍且不在赛程内）。本条保留原定义，待赛后重启。
2. **F-02 脱敏清洗（两段式）**：写入由 Agent 完成，脱敏分两段——① **贡献者 Agent 自检**：其 Agent 提交前依据脱敏规则先洗掉真名/密钥/内网路径/绝对路径等（Agent 有原始上下文，最知道什么敏感）；② **本站二次校验**：写入 API 落地前跑正则 + 审计 Agent 复检，命中敏感模式则拒收或隔离待人工抽检。人**不手动投稿**，故无"网页表单直接落库"风险。<br>📌 **本期简化**：当前为单人 git 策展库，写入 API 未部署，该流程尚未端到端跑通（脱敏规则本身在 `recipe.schema.md` 中已定稿）。
3. **F-03 经验列表与详情（人只读）**：网站展示所有已发布配方，可按领域标签筛选、关键词搜索；人无写入入口。
4. **F-04 机器可读接口**：自动生成 `/llms.txt`（索引）+ `/api/experiences.json`（全量正文），每次新投稿重写。
5. **F-05 演示 Agent「经验向导」**：用 **WorkBuddy** 搭建，学生口述卡点 → 检索本库(llms.txt/JSON) → 返回破局法 + 死胡同提醒。<br>✅ **已实现并升级**：实际交付为「MCP 内置 prompts（`experience-guide`）+ `mcp/AGENT_PROMPT.md` 纯文本兜底」，实现"任意接入的 Agent 一连即自动上岗"。细化规格见 v1.2 §7。
6. **F-06 贡献者表彰**：展示贡献次数/致谢榜（借鉴清华"清小搭"永久表彰模式，按 Agent/匿名标识计）。<br>⏸ **本期不做**：与"能被调用"主线无关（v1.2 §3 非目标）。
7. **F-10 只读 MCP Server**：让 Agent 以 MCP 协议查询本库。<br>⬆️ **已从 P2 提升为 P0**（原判"非必需"被推翻，见 v1.2 §1.1 LLM Agent 是反应式的、不会主动"想起"来读本库；`llms.txt` 只解决"能被读"，MCP 解决"会被调用"）。<br>✅ **已实现**：`mcp/server.js`，4 个工具（search_recipes / get_recipe / list_tags / list_quarantine）。细化规格见 v1.2 §7；**与 v1.1 冲突时以 v1.2 为准**。

### P1（决赛前，增强体验）
8. **F-07 主动惊喜推送**：**后续再说**（暂缓，不在 MVP 范围；待核心闭环验证后再评估是否加入）。
9. **F-08 相似配方聚类**：同问题多配方自动聚合，展示"主流破局路径"。<br>⏳ 本期**未做**（R-9，赛后）。
10. **F-09 反馈闭环**：消费者标记"这条对我有用/没用"，反哺排序。<br>⏳ 本期**未做**（依赖 M5 真实用户回流）。

### P2（赛后/有技术队友时）
11. **F-11 CLI 工具**：`jnu-recipe search "反爬"` 类命令行。<br>⏳ 本期**未做**。
12. **F-12 GitHub 开源仓库**：含写入模板、schema、示例数据，社区可 PR。<br>⏳ 本期**未做**（初赛前需完成 README / 说明书，开源排在说明书之后）。

> 📌 **F-10 提 P0 的连锁修订**：原 §8「机器可读架构」中"为何不用自研 MCP（MVP 阶段）…留 P2"整段结论已失效，同步重写为当前真实架构（见 §8）。

---

## 7. 数据模型：解题配方 Schema（核心技术规格）

每条配方 = 一个 SKILL.md 兼容的 Markdown 文件 + 一条 JSON 记录。

### 7.1 字段定义（引用 v3.0）

> **字段规范以 `recipe.schema.md` (v3.0) 为唯一权威来源**，本文档不再内联定义。v3.0 要点：
> - 单一真值源 = Markdown 文件头部的 YAML frontmatter；正文仅作叙事补充，机器不强制解析正文。
> - 死胡同（dead_ends）结构化进 frontmatter 的 YAML 数组，每条含 `attempt / failure / duration / early_signal`。
> - 写入契约 = JSON：贡献者 Agent 经写入 API 提交 JSON，系统渲染为 `.md` + 重建 `llms.txt` / `api/experiences.json`。
> - 必填 8 字段：`id / title / tags / model / problem / dead_ends[] / solution / status`。
> - 完整字段表、JSON 示例、渲染后 .md 示例、脱敏治理、开放边界见 `recipe.schema.md` 与 `Schema设计_PRD_v3.md`。

### 7.2 写入 API 负载（引用 v3.0）

- 负载格式：**JSON**，字段同 `recipe.schema.md` v3.0 表。
- `contributor_id` 与 `created_at` 由系统在接收时生成（贡献者无需填）。
- 校验：缺必填 / 类型错 / id 撞车 → 拒绝（不污染库）。
- 脱敏：贡献者 Agent 自检 + API 复检，均过才 `status: published`。
- 与旧版差异：砍掉 `summary/difficulty/time_spent/scrubbed/related/license/models_extra`；新增 `result/verified/contributor_id`；`status` 单字段表达治理（合并 scrubbed）；`hardware/agent_config` 改可选；写入契约从"Agent 写 .md"改为"Agent 交 JSON，系统渲染"。

---

## 8. 机器可读架构

> ⚠️ **本节约于 2026-09-25 回写**：原稿结论「不用自研 MCP，留 P2」已被 v1.2 §1.1 推翻（LLM Agent 是反应式的，不会主动"想起"来读本库；`llms.txt` 只解决"能被读"，MCP 解决"会被调用"）。F-10 已提 P0 并交付，故整节由"为什么不这样做"改写为"现在真实长什么样"。精细规格见 v1.2 §6/§7，冲突以 v1.2 为准。

### 8.1 交付给机器的静态四层（GitHub Pages，零依赖）

```
站点根
├── /llms.txt              # 索引：站点说明 + 每条配方链接与一句话描述
├── /api/experiences.json  # 全量正文（Agent 一次抓取得全库，44 条 / raw 120 KB / gzip 41.7 KB）
├── /recipes/<slug>.md     # 每条配方详情（YAML frontmatter + 叙事正文，SKILL.md 兼容）
└── /                      # 人类浏览的网页（列表+搜索+详情，只读）
```

`llms.txt` 真实开头（2026-09-24）：
```
# 暨南解题配方库 (agent-recipe-book)
> 人类解题经验的机器可读共享库。每条配方含 模型/skill/harness/硬件 与结构化死胡同。

## 索引
- recipes/recipe-git-pat-push.md: 用 fine-grained PAT 推 GitHub：Bearer 头被 git 忽略，必须 URL 内嵌 token 当 Basic Auth — ① git smart-HTTP 用 Basic Auth，Bearer 头无效
```

### 8.2 MCP Server（F-10，P0，已交付）

`mcp/server.js`——stdio JSON-RPC，零依赖、零上传、无需 API Key。4 个工具 + 1 套内置 prompts：

| 工具 | 入参 | 出参要点 | 说明 |
|---|---|---|---|
| `search_recipes` | `query`（必填）、`tags?`（数组，AND 语义）、`limit?`、`offset?`、`include_quarantine?` | `results[]`（含 `matchPct` / `confidence` / `suspect`）、`returned`、`has_more`、`matched` | 需翻页确认后才可下"库里没有"的结论 |
| `get_recipe` | `id` | 单条配方全文 | 按 id 精确取 |
| `list_tags` | 无 | 可用分组与标签 | 供上层做筛选器 |
| `list_quarantine` | `query?`（可选，给隔离项按匹配度排序） | C 级隔离项清单 | 审计视图，供人工复核；**只读，不提供放行操作**（放行由人在库侧完成） |

外加 `prompts/get(experience-guide)`：把「经验向导」的规矩固化进 server，**接入方不带任何自定义 Prompt 也能自动上岗**；`mcp/AGENT_PROMPT.md` 为纯文本兜底。

### 8.3 检索内核与防编造护栏（全程无 LLM 参与，故结果可复现、可离线）

- **打分**（`lib/search.js`）：中文 2-gram + 同义词表加权——标题 ×2、正文 ×1、同义词 tag ×3、元配方 ×0.35；实测单次 **0.111 ms**。
- **L3 分级置信**：A ×1.0 / B ×0.85 / C ×0（默认隔离）。C 级若匹配度 ≥ `C_LEVEL_MATCH_THRESHOLD`（默认 0.8，由 `_calibrate_match_threshold.js` 以 8 条正例标定，0.8/0.85/0.9 三档真噪声率均 0%）且为首位命中，破例放行并标 `suspect: true`。**阈值自管，不作为工具参数开放给调用方**。
- **防编造**（`lib/verifyCitations.js`）：正则校验答案中的 `[recipe-XXX]`，越界引用全拦，保证零库外内容。
- **分页**：`offset`（默认 0，上限 40，超限返回 JSON-RPC `-32602`）+ `limit`，返回 `has_more`。

### 8.4 为什么两层都要

| 维度 | `llms.txt` + JSON | MCP Server |
|---|---|---|
| 解决什么 | "能被读"（被动可达） | "会被调用"（主动可用） |
| 谁用 | 会主动抓静态资源的 Agent / 人 | 任何挂载本 server 的 MCP 客户端 |
| 代价 | 0（静态站自带） | 一个 stdio 进程 + 40 行 server |

> LLM Agent 是**反应式**的：它用当前上下文 + 已挂载工具解决问题，不会"想起"去访问某个网站（本项目实证见 v1.2 §1.1——开发中的 3 次卡点，都是自己解决完才写进库的）。因此 MCP 不是可选炫技项，而是本库被真正用起来的唯一路径。

---

## 9. 非功能需求

- **访问模型**：网站对**人只读**，写入**仅限 Agent 经 API**（核心原则，贯穿全文档）。
- **性能**：JSON 接口响应 < 500ms；网页首屏 < 2s。
- **可访问性**：全站无需登录即可读（Agent 与游客均可）。
- **可维护性**：存储层 + 静态生成器 + 写入 API 的低代码/少量脚本实现。
- **合规**：所有公开内容经两段式脱敏；标注"AI 辅助经验，非官方认定"。
- **开放协议**：MIT（见 §11）。

---

## 10. 开放边界与合规（红线）

| 开放（全公开） | 封闭（一律清洗） |
|---|---|
| 模型 / skill / harness | 真名、学号 |
| 脱敏后的 soul / Agent.md | API Key、Token、密码 |
| 电脑配置（硬件规格） | 内网 URL、内部系统路径 |
| 问题 / 死胡同 / 解法 / 复盘 | 含他人隐私的聊天原文 |
| 领域标签 / 贡献者昵称 | 文件系统绝对路径（如 C:\Users\...） |

**脱敏执行（两段式）**：① 贡献者 Agent 提交前自检清洗（有原始上下文，最知敏感）② 写入 API 二次正则 + 审计 Agent 复检，命中敏感模式拒收/隔离。人无从手填，泄漏面最小。
**责任声明**：本库为"提示助手"非"裁决者"，每条结论附"以官方/原作者核实为准"。

---

## 11. 开源策略

- **必须开源**：概念前提是"让所有 Agent 浏览"，闭源自相矛盾；且开源是参赛 maturity signal（MoonBit/RoboMaster 均倡导）。
- **协议**：**MIT**（传播优先，允许 fork 与商用，仅需保留版权声明）。
- **护城河不在代码**：在「就业办官方背书 + 人工策展 + 实时数据」，代码被 fork 不影响这三样。
- **源码范围**：网站 + 写入 API + 投稿/写入模板 + schema + 示例数据 + 脱敏脚本。

---

## 12. 技术架构与 MVP 实现路径

```
[贡献者 Agent] ──(写入API, 已脱敏)──▶ [存储层] ──▶ [静态生成器]
                                            │          ├─ 网页 (人只读浏览)
                                            │          ├─ /llms.txt
                                            │          └─ /api/experiences.json
[WorkBuddy 经验向导] ◀── 检索 JSON/llms.txt ◀── 学生提问
```

- **写入侧（Agent-only）**：贡献者的 Agent 在对话中提炼配方、自检脱敏后，调用本库**写入 API** 提交；人不能直接手填表单。
- **存储层（不限定飞书）**：飞书多维表只是早期设想的低代码便利项，**并非必需**。可选项：① 飞书多维表（低代码、易协作）② 轻量数据库(SQLite/Postgres) ③ Git 仓库(每条配方一个 .md，天然版本化+开源友好)。**MVP 推荐 Git 仓库 + 简单写入 API**，最贴合"开源 + Agent 可读 + 版本化"定位。
- **读取侧**：静态生成器把存储层渲染成网页 + llms.txt + JSON；演示用 **WorkBuddy** 搭「经验向导」，检索 JSON/llms.txt 返回破局法+死胡同。
- **零代码占比**：展示/演示 Agent 低代码；写入 API + 静态生成器需少量脚本。

### 12.1 存储架构演进路线（Git 不是终点，而是当前最优点）

**常见误解澄清**：纯文本 Markdown 配方体积极小——单条约 2–5 KB，即便累积 1 万条也仅 20–50 MB，clone 不会"塞爆电脑"。会撑爆仓库的只有**模型权重/数据集/视频等二进制大文件**，而本库设计明确不收纳此类资产。因此"MVP 用 Git"不等于"技术债"，而是对当前规模的最优解。

**分层原则**：写端（高频、并发、需权限/审计、含大文件）与读端（Agent 免鉴权、轻量、符合 llms.txt 标准）职责分离。Git 在 MVP 阶段兼任读写；规模期退居"只读发布镜像"。

| 阶段 | 内容规模 | 写端存储 | 读端（Agent 读） | 大文件处理 |
|---|---|---|---|---|
| **MVP（当前/初赛）** | < 100 条 | Git 仓库（每条 .md + `ingest.py` 渲染） | GitHub + `llms.txt` + `experiences.json` | 不入仓；仅引用外链 URL |
| **规模期（赛后）** | 万级 + 检索 | 数据库（SQLite 单机 / Postgres 联网） | 数据库导出 → 同构 Git 只读镜像 | 对象存储（S3 / Cloudflare R2）+ 数据库存元数据 |
| **多人协作期** | 并发写 + 权限 | 数据库 + 写入 API（鉴权/审计） | 同上 | 同上 |

**演进触发条件**（满足任一项即启动规模期改造）：
1. 配方条数 > 1000，且需按 `tags`/`model`/`verified` 做高频检索过滤；
2. 出现用户上传截图/日志/轨迹等大文件（须走对象存储，禁入 Git）；
3. 多贡献者并发写入，PR 流程成为瓶颈（需鉴权写入 API + 数据库）。

**为什么不全盘上数据库**：竞赛 Demo 阶段，数据库引入部署/运维成本却换不来"Agent 可直接读、开源可 Fork、零服务器"三大差异化优势；Git-native 反而让评委一眼看懂"机器可读开源知识库"的本质。数据库是**规模期的写入后端**，不是 MVP 的必选项。

**实现提示（赛后工程）**：写端轻量选 SQLite + 本地文件（单机够用）；联网多人用 Postgres + R2。定时任务把数据库导出为 `llms.txt`/`experiences.json` 并推送 Git 只读镜像，读链路架构完全不变。

---

## 13. 里程碑排期（距初赛 10/8 约 15 天）

| 阶段 | 日期 | 交付 |
|---|---|---|
| D1–D3 | 9/23–9/25 | 定 schema + 存储层字段 + 脱敏正则 + 写入 API 设计 |
| D4–D7 | 9/26–9/29 | 写入 API + 静态生成器产出网页 + llms.txt + JSON |
| D8–D10 | 9/30–10/2 | WorkBuddy「经验向导」搭建 + 3 条 Demo 脚本 |
| D11–D12 | 10/3–10/4 | 征集 ≥30 条真实配方（经 Agent 写入）+ 脱敏复核 |
| D13–D14 | 10/5–10/6 | 试用 ≥10 人收反馈 + 写说明书 |
| D15 | 10/7 | 录屏 Demo + 提交 PDF（截止 10/8） |

---

## 14. 风险与缓解

| 风险 | 缓解 |
|---|---|
| 内容不足（<30 条） | 团队先写自己踩坑 + 发动就业办/社团用 Agent 写入 |
| Demo 显不出差异化 | 演示 Agent 重点展示"返回死胡同提醒"而非普通问答 |
| 脱敏遗漏致泄密 | 贡献者 Agent 自检 + 本站正则/审计 Agent 双审；发布前预览 |
| 误称"首创"被答辩戳穿 | 表述精确为"首个校园/人类贡献/过程捕获版" |
| ~~MCP/CLI 工期吞噬~~ → 已反转 | MCP 已交付且成本极低（一个 stdio 进程 + 40 行，实测 0.111 ms/次）。**真实工期风险已让位于"库里内容不足"与"无真实用户验证"** |

---

## 15. 验收标准（映射 B 赛道评审权重）

| 评审维度 | 权重 | 原设想的得分点 | **实际交付（2026-09-25 对齐）** |
|---|---|---|---|
| 技术可行性 | 30% | Agent-only 写入 + 机器可读标准落地 | ✅ 只读 MCP Server 可跑（4 工具 + `experience-guide` prompts）；检索内核实测 **0.111 ms** / 次；6 套测试全绿 |
| 产品完整性 | 30% | 写入→审核→展示→Agent 查询闭环完整 | ⚠️ **可读侧完整**；写入侧为单人 git 策展（写入 API 未做，见 v1.2 §3 非目标）。答辩口径须如实，不要说"闭环完整" |
| 场景落地价值 | 40% | "解题经验开源"真实痛点 + 校园/官方背书 | 44 条真实配方、跨 4 组（采集 / 网站工程 / 项目判断 / 工具链）；**仍无真实用户验证（M5 未做，最大缺口）** |

**Demo 记忆点**：学生口述卡点 → Agent 返回"别人怎么破局 + 别踩哪些坑"→（F-07 暂缓）。

> 📌 **2026-09-25 对齐**：初赛（10/8）只交「说明书 PDF ≤10 页 + 可选附件」；2 分钟 Demo 视频是**决赛（11/5）**要求。不要把时间耗在做视频上（依据 v1.2 §0）。

---

## 附录 A：演示 Agent「经验向导」对话脚本（示例）

> 学生：我用 requests 抓站点，直接 403 就以为被封了。
> 向导：查到 1 条相关配方（`recipe-py-antibot-stop-on-hit`）。
> 破局：① 第一条请求就用 `try/except` 包住 + 显式 `timeout=(5, 12)`（连/读超时分开）；② 命中判定别只看 status——`anti_bot = status in (403,444,429,503) or headers.get('cf-mitigated') or body 命中 ['just a moment'…]`。
> ⚠️ 死胡同提醒：该站是 Cloudflare + DataDome JS 挑战，**改 UA 无效**；别一看到 403 就换代理硬刚。

> 📌 **2026-09-25 补充**：这不再是"手写的演示脚本"，而是 MCP server 内置 `prompts.get('experience-guide')` 固化后的**默认行为**——接入方不带任何自定义 Prompt，挂载即可复现此回答（这正是 R-4「自动上岗」的验收口径）。
>
> ⚠️ 原稿引用的 `recipe-anti-crawl-hy3` 已不在库中（配方重构后 id 变更），现按上表引真实 id。

## 附录 B：写入模板（供贡献者 Agent 调用）

见 §7.1 字段，由贡献者 Agent 组装为 JSON 后经**写入 API** 提交；人不直接操作。

---

*本 PRD v1.1 由 v1.0 修订：采纳"Agent 写入/人只读、两段式脱敏、WorkBuddy 搭建、存储不绑定飞书、死胡同结构化、F-07 暂缓、删 1.3"等意见。下步可产出：写入 API + 脱敏脚本设计 / WorkBuddy 经验向导搭建清单。*
